import {useState, useEffect} from 'react'

/* export const MessageComponent = () => {
    const [info, setInfo] = useState({name:'luis', lastName : 'lopez'})
    useEffect(() => {
        console.log('Called after each render');
        return () => {
            console.log('called on destroy')
        }
    })
    return (
        <div>
         <h4>{info.name}|{info.lastName}</h4>   
        </div>
    )
}

export const MyFirstUpdateEffect = ()=> {

} */